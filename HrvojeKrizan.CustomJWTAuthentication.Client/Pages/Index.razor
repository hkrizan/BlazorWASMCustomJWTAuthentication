@page "/"
@attribute [Authorize]

<PageTitle>Index</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

<AuthorizeView>
    <NotAuthorized>
        <h1>NOT AUTHORIZED!!!!</h1>
    </NotAuthorized>
</AuthorizeView>

<AuthorizeView Roles="Admin">
    <p>This is visible by "Admin" role only</p>
</AuthorizeView>

<AuthorizeView Roles="User">
    <p>This is visible by "User" role only</p>
</AuthorizeView>

<AuthorizeView Policy="SuperAdmin">
    <p>This is visible if role or user has "SuperAdmin" claim</p>
</AuthorizeView>

<AuthorizeView Policy="SpecialPermissions">
    <p>This is visible if role or user has "SpecialPermissions" claim</p>
</AuthorizeView>